import type { Layout } from "../types";

export const layout: Readonly<Layout> = Object.freeze({
    width: 1200,
    height: 620,
    left: 160,
    right: 20,
    top: 34,
    bottom: 36,
    laneGap: 16
});
